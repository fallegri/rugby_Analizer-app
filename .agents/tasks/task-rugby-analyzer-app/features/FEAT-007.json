{
  "id": "FEAT-007",
  "type": "fix",
  "description": "Fix VideoProcessor instantiation and error propagation: Instantiate a real VideoProcessor with YOLODetector, MultiObjectTracker, TrackingStrategy, and AnalyticsEngine in the start_analysis endpoint. Send WebSocket error messages when processing fails (including when video_processor is None). Add detailed console logging with phase, frame, percentage, and FPS during processing.",
  "status": "completed",
  "steps": [
    "Modify backend/src/api/routes/analysis.py: Instantiate VideoProcessor with YOLOv8n detector, ByteTrack tracker, appropriate TrackingStrategy based on mode, and AnalyticsEngine. Wrap in try/except to catch model loading failures and return error to frontend.",
    "Modify backend/src/services/background_tasks.py: When video_processor is None, send WebSocket error before returning. Add detailed console logging at each processing stage (loading, detecting, tracking, calibrating, analytics, complete) with frame counts, percentages, and FPS.",
    "Verify frontend AnalysisPage.tsx already handles WebSocket error type messages correctly (it does - no changes needed).",
    "Modify backend/src/api/routes/video.py: Unify the frontend-backend flow by making POST /api/video/{video_id}/process instantiate the full CV pipeline, start BackgroundTaskManager, and return session_id instead of ProcessingResult.",
    "Update ProcessRequest model to accept frontend's payload (target_player_ids as strings, calibration as full object).",
    "Add integration tests for the unified video process flow."
  ],
  "acceptance_criteria": [
    "VideoProcessor is properly instantiated with YOLODetector(yolov8n.pt), MultiObjectTracker, appropriate TrackingStrategy, and AnalyticsEngine",
    "If VideoProcessor instantiation fails (e.g., model not loadable, missing dependencies), the error is caught and sent via WebSocket to frontend",
    "When video_processor is None in background_tasks.py, a WebSocket error message is sent before marking the session failed",
    "Console logging shows phase name, current frame/total frames, percentage, and processing FPS",
    "Frontend displays error messages from WebSocket (already implemented, verified working)",
    "POST /api/video/{video_id}/process returns { session_id } that frontend can use for WebSocket connection",
    "The endpoint starts the BackgroundTaskManager with a real VideoProcessor",
    "All existing tests pass"
  ],
  "verification": [
    "cd /projects/sandbox/rugby_Analizer-app/backend && uv run pytest tests/ -v",
    "cd /projects/sandbox/rugby_Analizer-app/frontend && npm run build"
  ],
  "blocked_reason": null,
  "findings": "All 215 backend tests pass (5 new integration tests added for the video process endpoint). Frontend build succeeds (TypeScript compilation verified). The core issue was that frontend's startProcessing() called POST /api/video/{videoId}/process but that endpoint only set status to ANALYZING and returned ProcessingResult without session_id. The real processing was in POST /api/analysis/start which frontend never called. Fixed by making the video process endpoint internally instantiate the full CV pipeline, start BackgroundTaskManager, and return { session_id }. The frontend code already handled session_id correctly and needed no changes."
}
